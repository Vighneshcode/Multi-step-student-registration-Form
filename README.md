# Multi-step-student-registration-Form
"This repository contains a fully responsive, multi-step web application for student registration and account setup. It demonstrates clean coding practices, semantic HTML structure (&lt;fieldset>, &lt;legend>), and client-side form validations using HTML5 patterns (Regex) without relying on external libraries."
